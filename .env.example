# Used by docker-compose.yml. Copy to .env and fill in a real secret before
# running `docker compose up`.
#
# Generate one with:
#   openssl rand -base64 32
SESSION_SECRET=replace-with-a-long-random-string
